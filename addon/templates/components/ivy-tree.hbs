{{#each node.childNodes as |childNode|}}
  {{#ivy-tree-treeitem activate=activate activeNode=activeNode expand=(action "expand") node=childNode tree=this as |descendant|}}
    {{yield descendant}}
  {{/ivy-tree-treeitem}}
{{/each}}
