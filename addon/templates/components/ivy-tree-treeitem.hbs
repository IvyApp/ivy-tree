{{yield node}}

{{#if node.hasChildNodes}}
  {{#component groupComponent
    activate=activate
    activeNode=activeNode
    expand=expand
    groupComponent=groupComponent
    isVisible=node.expanded
    node=node
    tree=tree
    treeitemComponent=treeitemComponent as |descendantNode|}}
    {{yield descendantNode}}
  {{/component}}
{{/if}}
