{{yield node}}

{{#if node.hasChildNodes}}
  {{#ivy-tree-group activate=activate activeNode=activeNode expand=expand isVisible=node.expanded node=node tree=tree as |descendant|}}
    {{yield descendant}}
  {{/ivy-tree-group}}
{{/if}}
